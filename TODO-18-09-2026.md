# TODO — 18/09/2026

## 🎯 Principal : vérifier l'appairage sur le SaaS (en vrai, via l'UI)

Tester **depuis le dashboard partner** (pas juste l'API) que les règles d'appairage tiennent :

- [ ] **On ne peut PAS appairer une app coworking sur un écran déclaré legacy/Airbnb** (et inversement legacy sur écran Coworking) → message de refus affiché côté partner.
- [ ] **Exclusion mutuelle** : une box déjà appairée en tant que legacy refuse l'app coworking (message *« déjà appairé en tant que NeoFilm TV… contactez le support »*) et inversement.
- [ ] **Séparation** : la même box (même androidId) peut porter 1 appairage legacy **OU** 1 coworking, sur des écrans différents — mais pas les deux appairés en même temps (cf. exclusion).
- [ ] **Cas positif** : coworking → écran Coworking ✅, legacy → écran Airbnb ✅ (l'appairage normal marche toujours).
- [ ] **PIN** : jamais deux codes identiques actifs en même temps.

### Fait via ADB/box, à refaire proprement dans l'UI partner
Tests faits en simulation API + sur le Fire Stick (192.168.1.175) — à confirmer via le vrai parcours partenaire (`localhost:3002` → Écrans → Appairage → saisie du PIN).

---

## ⚠️ État du code (au 17/09 soir)

- ✅ **Poussé sur `dev`** (commit `698cd58`) : séparation appairage par `appVariant` (schéma + migration + adoption paresseuse) + fix vidéo `object-fit: contain`.
- ⏳ **PAS ENCORE COMMITÉ** (sur disque + déployé sur staging seulement) :
  - **Exclusion mutuelle** (une seule app appairée par box) — `tv-auth.service.ts` (`assertNoOtherVariantPaired`).
  - **Règle catégorie** (app ↔ `Screen.usage` : legacy↔AIRBNB, coworking↔COWORKING) — `tv-auth.service.ts` (check dans `pairByPin`).
  - → **À committer + push sur `dev`** demain (un seul commit).

## 🔧 Ops en attente
- [ ] **Réactiver Play Integrity sur staging** (coupé pour les tests d'appairage — sinon anti-VM inactif sur staging).
- [ ] **Prod** : quand validé → appliquer la migration DB (`20260917120000_device_app_variant_pairing`) + déployer images `:latest` (api, tv-legacy, coworking). Rien d'auto, sur validation explicite.
- [ ] Nettoyer d'éventuelles lignes de test `SIM-%` restantes sur staging.

## 📌 Rappels de contexte
- Base staging : conteneur `neofilm-staging-postgres-1` (user `postgres`, db `neofilm`).
- Fire Stick de test : `192.168.1.175` (adb), androidId `17b60a776b798082`.
- Mapping : app **legacy** = NeoFilm TV = écran **AIRBNB** ; app **coworking** = écran **COWORKING**.
