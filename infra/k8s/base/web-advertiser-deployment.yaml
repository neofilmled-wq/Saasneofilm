apiVersion: apps/v1
kind: Deployment
metadata:
  name: neofilm-web-advertiser
  namespace: neofilm
  labels:
    app: neofilm-web-advertiser
spec:
  replicas: 2
  selector:
    matchLabels:
      app: neofilm-web-advertiser
  template:
    metadata:
      labels:
        app: neofilm-web-advertiser
    spec:
      containers:
        - name: web-advertiser
          image: neofilm/web-advertiser:latest
          ports:
            - containerPort: 3003
              name: http
          env:
            - name: NEXT_PUBLIC_API_URL
              valueFrom:
                configMapKeyRef:
                  name: neofilm-config
                  key: API_URL
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /
              port: 3003
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: neofilm-web-advertiser
  namespace: neofilm
spec:
  selector:
    app: neofilm-web-advertiser
  ports:
    - port: 3003
      targetPort: 3003
      name: http
  type: ClusterIP
