apiVersion: apps/v1
kind: Deployment
metadata:
  name: neofilm-web-partner
  namespace: neofilm
  labels:
    app: neofilm-web-partner
spec:
  replicas: 2
  selector:
    matchLabels:
      app: neofilm-web-partner
  template:
    metadata:
      labels:
        app: neofilm-web-partner
    spec:
      containers:
        - name: web-partner
          image: neofilm/web-partner:latest
          ports:
            - containerPort: 3002
              name: http
          env:
            - name: NEXT_PUBLIC_API_URL
              valueFrom:
                configMapKeyRef:
                  name: neofilm-config
                  key: API_URL
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /
              port: 3002
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: neofilm-web-partner
  namespace: neofilm
spec:
  selector:
    app: neofilm-web-partner
  ports:
    - port: 3002
      targetPort: 3002
      name: http
  type: ClusterIP
